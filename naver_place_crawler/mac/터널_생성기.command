#!/bin/bash

echo "========================================================="
echo "        임시 인터넷 주소(터널) 자동 생성기"
echo "========================================================="
echo "※ 주의: 먼저 '서버_및_관리자_실행.command'를 켜두셔야 합니다."
echo ""
echo "아래 화면에 잠시 후 나타나는 주소 중"
echo "👉 https://xxxxxx.lhr.life 👈 모양의 주소를 복사해서"
echo "앱을 설치한 사용자에게 카카오톡 등으로 전달해주세요!"
echo "========================================================="
echo ""
echo "터널을 연결합니다... (종료하려면 창을 닫으세요)"

# 별도의 설치 없이 Mac 내장 SSH를 이용하여 localhost.run 터널링 수행
ssh -o StrictHostKeyChecking=no -R 80:localhost:8005 nokey@localhost.run
